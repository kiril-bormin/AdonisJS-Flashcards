@component('components/layout', { title: title })
  <div class="play-container">

    @if(!showAnswer)
      <div class="card-box">
        <p class="card-label">
          Question
        </p>
        <p class="card-content">
          {{ card.front }}
        </p>
      </div>
      <div class="play-actions">
        <a
          href="{{ route('play.play', { id: deckId }) }}?mode={{ mode }}&index={{ index }}&showAnswer=true"
          class="add-button"
        >Voir la réponse</a>
      </div>
    @else
      <div class="card-box">
        <p class="card-label">
          Réponse
        </p>
        <p class="card-content">
          {{ card.back }}
        </p>
      </div>
      <div class="play-actions">
        <a
          href="{{ route('play.play', { id: deckId }) }}?mode={{ mode }}&index={{ index }}&showAnswer=false"
          class="add-button btn-juste"
        >Voir la question</a>
        <a
          href="{{ route('play.play', { id: deckId }) }}?mode={{ mode }}&index={{ index + 1 }}&showAnswer=false"
          class="add-button btn-juste"
        >Prochain</a>

      </div>
    @end
    
  </div>

  <style>
    .play-container {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 60vh;
      font-family: sans-serif;
    }
    
    .card-box {
      border: 2px solid #ccc;
      border-radius: 5px;
      padding: 40px 60px;
      margin-bottom: 40px;
      background: #fff;
    }
    
    .card-label {
      font-weight: bold;
      font-size: 20px;
      margin-bottom: 16px;
    }
    
    .card-content {
      font-size: 18px;
      color: #333;
    }
    
    .play-actions {
      display: flex;
      gap: 40px;
      justify-content: center;
    }
    
    .progress {
      margin-top: 30px;
      color: #888;
      font-size: 14px;
    }
  </style>
@endcomponent
